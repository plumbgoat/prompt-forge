---
description: Build, improve, or test a prompt — then save it as a template or slash command
argument-hint: "[describe the task | paste a prompt to improve | 'test' to battle-test one]"
---

Use the prompt-engineering skill. The user's input: $ARGUMENTS

Detect the mode: a task description → BUILD (short interview, then forge);
a pasted prompt → IMPROVE (checklist critique, explained rewrite); "test" or
a prompt plus sample inputs → TEST (run it against real inputs, fix, re-run).
Always offer to save the finished prompt to the project prompt library or as
a reusable slash command.
